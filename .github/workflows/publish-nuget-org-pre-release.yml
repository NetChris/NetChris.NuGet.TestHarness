name: Pre-Release - NuGet

on:
  release:
    types: [prereleased]

jobs:
  push:
    permissions:
      contents: read
      packages: write
    # TODO - SHA
    uses: NetChris/workflows/.github/workflows/pre-release-nuget-org.yml@separate-workflows-for-nuget-org
    secrets: inherit
